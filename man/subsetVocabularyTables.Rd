% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocabularySubset.R
\name{subsetVocabularyTables}
\alias{subsetVocabularyTables}
\title{Subset vocabulary tables in a CDM}
\usage{
subsetVocabularyTables(
  cdm = NULL,
  conceptSet = NULL,
  cdmTables = NULL,
  includeRelated = TRUE,
  keepDomains = c("Unit", "Visit", "Gender")
)
}
\arguments{
\item{cdm}{A \code{cdm_reference} object used as the base structure to update.}

\item{conceptSet}{Numeric vector of concept IDs to retain.}

\item{cdmTables}{Optional named list of vocabulary tables to subset instead
of a full \code{cdm_reference}. This is mainly used internally.}

\item{includeRelated}{Whether to retain concepts directly related to
\code{conceptSet}. Defaults to \code{TRUE}.}

\item{keepDomains}{Character vector of \code{domain_id} values to always retain
when subsetting vocabulary tables. Defaults to
\code{c("Unit", "Visit", "Gender")}.}
}
\value{
A modified \code{cdm_reference} object.
}
\description{
Restricts the vocabulary tables in a \code{cdm_reference} to a target concept set
while optionally retaining directly related concepts and selected
\code{domain_id} values. Non-vocabulary OMOP tables are then filtered so rows that
reference removed concepts are also dropped.
}
\examples{
\donttest{
cdm <- mockCdmFromDataset()
cdm <- cdm |> subsetVocabularyTables(conceptSet = c(35208414))
}
}
